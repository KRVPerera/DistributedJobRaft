package model

// Message represents the format of a message in the distributed system
type Message struct {
	// Add fields as per your message format
}

// Job represents a job in the job queue
type Job struct {
	// Add fields as per your job format
}

// Node represents a single node in the distributed system
type Node struct {
	// Add fields as per your node requirements
}

// Socket represents the socket for communication between nodes
type Socket struct {
	// Add socket fields here
}
